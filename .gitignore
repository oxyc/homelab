# secrets — never commit
.env
docker/.env
**/secrets.yaml
*.key
*.pem

# ansible
ansible/inventory.yml
ansible/group_vars/all.yml
*.retry
.vault_pass

# docs / planning — kept local only
docs/

# local / editor
.DS_Store
*.swp
