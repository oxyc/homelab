# docker_host defaults
homelab_dir: /opt/homelab
compose_src: "{{ role_path }}/../../../docker"   # repo docker/ dir (real + molecule)
compose_profiles: []          # add 'homekit' (in group_vars) to include Scrypted
molecule_test: false          # set true by the molecule scenario (no proxmox/docker daemon)
