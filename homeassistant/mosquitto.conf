# Mosquitto — LAN-only broker for Frigate <-> HA. Not internet-exposed (the HA container is on the LAN
# behind the router, no port-forward). Add auth with a password_file + mosquitto_passwd if you want it.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
